import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('AbobaApp', () => App);
AppRegistry.runApplication('AbobaApp', {
  rootTag: document.getElementById('root'),
});
